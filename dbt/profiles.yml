marketfeed:
  outputs:
    dev:
      type: duckdb
      path: ../data/marketfeed.duckdb
      schema: main
      threads: 4
      extensions:
        - httpfs
        - parquet
        - json
  target: dev
